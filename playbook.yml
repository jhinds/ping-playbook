---
- name: Create a text file
  hosts: all
  gather_facts: False
  tasks:
    - name: Create a text file
      shell: touch /tmp/test_file.txt
      tags:
        - taco
    - name: Intentionally a bad command
      shell: echo hi
      tags:
        - salad
